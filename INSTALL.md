# 🏗️ 샷시시공 통합관리 시스템 - 로컬 설치 가이드

> 컴퓨터 초보자도 쉽게 따라할 수 있는 단계별 설치 가이드입니다.

---

## 📋 목차
1. [시작하기 전에 준비할 것](#1-시작하기-전에-준비할-것)
2. [Node.js 설치하기](#2-nodejs-설치하기)
3. [프로젝트 다운로드](#3-프로젝트-다운로드)
4. [프로그램 실행하기](#4-프로그램-실행하기)
5. [문제 해결](#5-문제-해결)

---

## 1️⃣ 시작하기 전에 준비할 것

### 필요한 것들
- ✅ 컴퓨터 (Windows, Mac, Linux 모두 가능)
- ✅ 인터넷 연결
- ✅ 약 30분의 시간

### 설치할 프로그램
1. **Node.js** - 이 프로그램을 실행하는 데 필요한 기본 프로그램
2. **코드 에디터 (선택사항)** - VSCode 추천 (코드를 보고 싶을 때)

---

## 2️⃣ Node.js 설치하기

### Windows 사용자

**Step 1: Node.js 다운로드**
1. 인터넷 브라우저를 엽니다 (크롬, 엣지 등)
2. 주소창에 `https://nodejs.org` 입력
3. 큰 녹색 버튼 "LTS 권장 버전" 클릭
4. 다운로드된 파일(예: `node-v20.x.x-x64.msi`) 찾기

**Step 2: 설치 실행**
1. 다운로드한 파일을 더블클릭
2. "Next" 버튼 계속 클릭 (모두 기본 설정으로)
3. "I accept..." 체크박스 체크
4. "Install" 클릭
5. 설치 완료될 때까지 대기 (약 2-3분)
6. "Finish" 클릭

**Step 3: 설치 확인**
1. 키보드에서 `Windows 키 + R` 누르기
2. `cmd` 입력하고 Enter
3. 검은 화면(명령 프롬프트)이 나타남
4. 다음 명령어 입력:
   ```
   node --version
   ```
5. 버전 번호가 나오면 성공! (예: `v20.11.0`)

### Mac 사용자

**Step 1: Node.js 다운로드**
1. Safari나 크롬 브라우저 열기
2. `https://nodejs.org` 방문
3. "LTS 권장 버전" 다운로드
4. 다운로드 폴더에서 `.pkg` 파일 찾기

**Step 2: 설치 실행**
1. 다운로드한 `.pkg` 파일 더블클릭
2. "계속" 버튼 클릭
3. "동의함" 클릭
4. 비밀번호 입력 (Mac 로그인 비밀번호)
5. 설치 완료까지 대기
6. "닫기" 클릭

**Step 3: 설치 확인**
1. `Spotlight` 검색 (Command + Space)
2. `terminal` 입력하고 Enter
3. 터미널 창이 나타남
4. 다음 명령어 입력:
   ```
   node --version
   ```
5. 버전 번호가 나오면 성공!

---

## 3️⃣ 프로젝트 다운로드

### 방법 1: ZIP 파일로 다운로드 (가장 쉬움! ⭐ 추천)

**Step 1: 프로젝트 파일 다운로드**

현재 프로젝트 파일은 샌드박스 서버에 있습니다. 
제가 ZIP 파일로 압축해서 다운로드 링크를 드리겠습니다.

**Step 2: 압축 풀기**
1. 다운로드된 ZIP 파일 찾기 (보통 다운로드 폴더)
2. 파일 우클릭 → "압축 풀기" 또는 "Extract"
3. 원하는 위치에 압축 풀기 (예: 바탕화면, 문서 폴더)

---

### 방법 2: Git으로 다운로드 (개발자용)

> Git이 이미 설치되어 있는 분들만 사용하세요

```bash
# 원하는 폴더로 이동 (예: 바탕화면)
cd Desktop

# GitHub에서 클론 (나중에 GitHub 주소로 변경)
git clone <GitHub-저장소-주소>

# 프로젝트 폴더로 이동
cd construction-management-system
```

---

## 4️⃣ 프로그램 실행하기

### Step 1: 명령 프롬프트/터미널 열기

**Windows:**
1. 압축 푼 폴더 열기
2. 폴더 안에서 주소창에 `cmd` 입력하고 Enter
3. 명령 프롬프트가 해당 폴더에서 열림

**Mac:**
1. 터미널 열기 (Command + Space, "terminal" 검색)
2. `cd` 명령어로 폴더 이동:
   ```bash
   cd Desktop/construction-management-system
   ```

### Step 2: 필요한 프로그램 설치 (처음 한 번만)

명령 프롬프트/터미널에 다음 명령어 입력:

```bash
npm install
```

**무슨 일이 일어나나요?**
- 프로그램 실행에 필요한 모든 파일을 자동으로 다운로드합니다
- 약 2-5분 정도 걸립니다
- `node_modules` 폴더가 생성됩니다 (약 200MB)
- 완료되면 다시 명령어를 입력할 수 있게 됩니다

**진행 상황:**
```
npm install
⠸ reify:@babel/types: timing reifyNode:node_modules/@babel/types Completed in 234ms
...
added 392 packages in 49s
```

이런 메시지들이 계속 나오면 정상입니다!

### Step 3: 프로그램 실행

명령 프롬프트/터미널에 다음 명령어 입력:

```bash
npm run dev
```

**성공하면 이런 메시지가 나타납니다:**

```
> construction-management-system@1.0.0 dev
> concurrently "npm run server" "npm run client"

[0] ✅ Database initialized successfully!
[0] 🚀 Server running on http://localhost:5000
[1] VITE v5.4.21  ready in 300 ms
[1] ➜  Local:   http://localhost:3000/
```

**이것은 무엇을 의미하나요?**
- `[0]` = 백엔드 서버 (데이터베이스 관리)
- `[1]` = 프론트엔드 서버 (화면 표시)
- 두 개의 서버가 동시에 실행됨

### Step 4: 브라우저에서 열기

**방법 1: 자동으로 열리는 경우**
- 일부 시스템에서는 브라우저가 자동으로 열립니다

**방법 2: 수동으로 열기**
1. 인터넷 브라우저 열기 (크롬, 엣지, 사파리 등)
2. 주소창에 입력: `http://localhost:3000`
3. Enter 키 누르기

**화면이 나타났나요? 🎉 축하합니다!**

---

## 5️⃣ 프로그램 사용하기

### 메뉴 설명

화면 상단에 5개의 메뉴가 있습니다:

1. **📊 대시보드**
   - 이번 달 시공 건수
   - 총 매출
   - 미정산 건수
   - 업체별 통계

2. **✏️ 시공내역 등록**
   - 새로운 시공 내역 입력
   - 날짜, 업체, 고객명, 주소 입력
   - 청구금액, 지급금액 입력
   - 부가작업 체크 (난간대, 방범창 등)

3. **📋 시공내역 조회**
   - 전체 시공 내역 확인
   - 기간별, 업체별 필터링
   - 수정, 삭제 가능

4. **📅 월간 스케줄**
   - 캘린더 형태로 시공 일정 확인
   - 업체별 색상으로 구분
   - 클릭하면 상세 내용 확인

5. **💰 정산 관리**
   - 업체별 정산 금액 자동 계산
   - 기간 선택 가능
   - 청구/지급 금액 비교

---

## 6️⃣ 프로그램 종료하기

### 서버 중지

**명령 프롬프트/터미널에서:**
1. `Ctrl + C` 키 누르기 (Windows/Mac 동일)
2. "Terminate batch job? (Y/N)" 나오면 `Y` 입력하고 Enter
3. 서버가 중지됩니다

**브라우저는 그냥 닫으면 됩니다!**

---

## 7️⃣ 다음에 다시 실행하려면?

### 매우 간단합니다!

1. **명령 프롬프트/터미널 열기**
   - Windows: 프로젝트 폴더에서 주소창에 `cmd`
   - Mac: 터미널에서 프로젝트 폴더로 이동

2. **실행 명령어 입력**
   ```bash
   npm run dev
   ```

3. **브라우저에서 열기**
   - `http://localhost:3000`

**`npm install`은 다시 안 해도 됩니다!**
(처음 한 번만 하면 됩니다)

---

## 8️⃣ 문제 해결 (FAQ)

### ❓ "npm: command not found" 오류

**원인:** Node.js가 제대로 설치되지 않음

**해결방법:**
1. Node.js 다시 설치 ([2단계](#2-nodejs-설치하기) 참고)
2. 명령 프롬프트/터미널 다시 열기
3. `node --version` 확인

---

### ❓ "Port 3000 is already in use" 오류

**원인:** 이미 다른 프로그램이 3000 포트 사용 중

**해결방법 1: 기존 프로세스 종료**

**Windows:**
```bash
# 3000 포트 사용하는 프로세스 찾기
netstat -ano | findstr :3000

# 프로세스 종료 (PID 번호는 위에서 확인)
taskkill /PID [PID번호] /F
```

**Mac:**
```bash
# 3000 포트 사용하는 프로세스 종료
lsof -ti:3000 | xargs kill -9
```

**해결방법 2: 다른 포트 사용**

`vite.config.js` 파일 열어서:
```javascript
server: {
  port: 3001,  // 3000 대신 3001 사용
```

---

### ❓ 화면이 안 나타나요

**체크리스트:**
1. ✅ 명령 프롬프트에 에러 메시지 없나요?
2. ✅ "Server running" 메시지 보이나요?
3. ✅ 브라우저 주소가 정확한가요? (`localhost:3000`)
4. ✅ 방화벽이 차단하지 않나요?

**해결방법:**
1. `Ctrl + C`로 서버 중지
2. `npm run dev` 다시 실행
3. 브라우저 새로고침 (F5)

---

### ❓ "Cannot find module" 오류

**원인:** 필요한 파일이 설치되지 않음

**해결방법:**
```bash
# node_modules 폴더 삭제
rm -rf node_modules  # Mac/Linux
rmdir /s node_modules  # Windows

# 다시 설치
npm install
```

---

### ❓ 데이터가 사라졌어요

**원인:** SQLite 데이터베이스 파일 위치

**데이터베이스 파일:**
- 위치: `server/construction.db`
- 이 파일이 있으면 데이터가 보존됩니다
- 백업하려면 이 파일을 복사하세요!

**데이터 백업 방법:**
1. 프로그램 종료
2. `server/construction.db` 파일 복사
3. 안전한 곳에 보관
4. 복원할 때는 다시 붙여넣기

---

## 9️⃣ 추가 팁

### 💡 개발자 도구 사용하기

**브라우저에서:**
1. `F12` 키 누르기 (또는 우클릭 → "검사")
2. "Console" 탭 클릭
3. 에러 메시지 확인 가능
4. API 요청/응답 확인 가능

### 💡 데이터 초기화하기

**샘플 데이터 다시 넣기:**
```bash
node server/seed.js
```

### 💡 포트 변경하기

**프론트엔드 포트 변경:**
- 파일: `vite.config.js`
- 라인: `port: 3000` → 원하는 포트로 변경

**백엔드 포트 변경:**
- 파일: `server/index.js`
- 라인: `PORT = 5000` → 원하는 포트로 변경

---

## 🔟 프로그램 업데이트

### 새 버전 다운로드

**GitHub에서 업데이트:**
```bash
# 최신 코드 받기
git pull origin main

# 새로운 패키지 설치
npm install

# 서버 재시작
npm run dev
```

---

## 📞 도움이 더 필요하신가요?

### 자주 사용하는 명령어 요약

```bash
# 패키지 설치 (처음 한 번)
npm install

# 프로그램 실행
npm run dev

# 프로그램 종료
Ctrl + C

# 샘플 데이터 추가
node server/seed.js
```

---

## ✅ 체크리스트

설치 전:
- [ ] Node.js 설치 완료
- [ ] 프로젝트 파일 다운로드 완료

실행 전:
- [ ] 프로젝트 폴더 위치 확인
- [ ] `npm install` 실행 완료

실행 중:
- [ ] 명령 프롬프트에 에러 없음
- [ ] "Server running" 메시지 확인
- [ ] 브라우저에서 화면 보임

---

**🎉 축하합니다! 이제 프로그램을 사용할 수 있습니다!**

궁금한 점이 있으면 언제든지 질문하세요! 😊
