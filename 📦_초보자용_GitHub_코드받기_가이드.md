# 📦 초보자용 GitHub 코드 받기 가이드

## 🎯 가장 쉬운 방법: ZIP 다운로드 (Git 불필요!)

### ✅ 1단계: GitHub 페이지 열기
1. 인터넷 브라우저(Chrome, Edge 등) 실행
2. 주소창에 아래 주소 복사 & 붙여넣기:
   ```
   https://github.com/giguestar/HMOA-CSS-PROJECT
   ```
3. Enter 키 누르기

---

### ✅ 2단계: 브랜치 선택 (중요!)
1. 페이지 왼쪽 위에 **`main`** 버튼 찾기 (회색 버튼)
2. 버튼 클릭
3. 드롭다운 메뉴에서 **`genspark_ai_developer`** 선택
   - 이 브랜치에 최신 코드가 있습니다!

---

### ✅ 3단계: ZIP 파일 다운로드
1. 페이지 오른쪽 위에 녹색 **`<> Code`** 버튼 찾기
2. 버튼 클릭
3. 메뉴 맨 아래 **`Download ZIP`** 클릭
4. 파일이 다운로드됩니다!
   - 파일명: `HMOA-CSS-PROJECT-genspark_ai_developer.zip`
   - 위치: 다운로드 폴더 (보통 `C:\Users\사용자명\Downloads\`)

---

### ✅ 4단계: 압축 해제
1. 다운로드 폴더 열기
2. `HMOA-CSS-PROJECT-genspark_ai_developer.zip` 파일 찾기
3. **오른쪽 클릭** → **압축 풀기** 또는 **Extract All** 선택
4. 압축 풀 위치 선택:
   - 추천: `C:\Projects\` (없으면 새로 만들기)
   - 또는 원하는 위치
5. **압축 풀기** 버튼 클릭
6. 완료!

**결과**: `C:\Projects\HMOA-CSS-PROJECT-genspark_ai_developer` 폴더 생성됨

---

## 🚀 실행 방법

### 📋 1단계: Node.js 설치 (처음 1회만!)

#### 1. Node.js 다운로드
- 웹사이트: https://nodejs.org/ko
- 왼쪽 **LTS** 버전 다운로드 (안정 버전)

#### 2. Node.js 설치
1. 다운로드된 파일 더블클릭
2. **Next** 버튼 계속 클릭 (기본 설정 사용)
3. **I accept the terms...** 체크
4. **Install** 클릭
5. 완료될 때까지 대기 (5분 정도)
6. **Finish** 클릭

#### 3. 설치 확인
1. **Windows 키 + R** 누르기
2. `cmd` 입력 → **Enter**
3. 명령 프롬프트에서 입력:
   ```
   node --version
   ```
4. 결과 확인: `v18.x.x` 또는 `v20.x.x` 표시되면 성공!

---

### 📋 2단계: 프로젝트 실행

#### 1. 명령 프롬프트 열기
- **Windows 키 + R** → `cmd` 입력 → **Enter**

#### 2. 프로젝트 폴더로 이동
```
cd C:\Projects\HMOA-CSS-PROJECT-genspark_ai_developer
```
*(실제 압축 해제한 경로로 변경하세요)*

💡 **팁**: 폴더를 찾기 어려우면:
1. 파일 탐색기에서 폴더 열기
2. 주소창 클릭 → `cmd` 입력 → Enter
3. 자동으로 해당 폴더에서 명령 프롬프트가 열립니다!

#### 3. 패키지 설치 (처음 1회만!)
```
npm install
```
- **Enter** 누르기
- 설치 진행됨 (5~10분 소요, 인터넷 속도에 따라 다름)
- 여러 줄의 텍스트가 표시되며 설치됩니다
- 완료되면 프롬프트(`>`)가 다시 나타납니다

#### 4. 실행!
```
npm run dev
```
- **Enter** 누르기
- 서버가 시작됩니다!
- 메시지 표시:
  ```
  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
  ```

#### 5. 브라우저에서 열기
1. Chrome, Edge 등 브라우저 실행
2. 주소창에 입력:
   ```
   http://localhost:3000
   ```
3. **Enter** 누르기
4. 로그인 화면이 나타나면 성공! 🎉

---

### 🔐 로그인 정보

#### 관리자 계정:
- **아이디**: `admin`
- **비밀번호**: `admin1234`

#### 매니저 계정:
- **이상무**: `lee` / `l1234`
- **정호규**: `jung` / `j1234`
- **김남군**: `kim` / `k1234`

---

## 🔄 다음부터 실행하는 방법 (간단!)

### 매번 컴퓨터를 켤 때마다:
1. **명령 프롬프트(cmd)** 열기
2. 프로젝트 폴더로 이동:
   ```
   cd C:\Projects\HMOA-CSS-PROJECT-genspark_ai_developer
   ```
3. 실행:
   ```
   npm run dev
   ```
4. 브라우저에서 `http://localhost:3000` 열기

💡 **팁**: 바탕화면에 바로가기 만들기:
1. 메모장 열기
2. 아래 내용 복사:
   ```batch
   @echo off
   cd C:\Projects\HMOA-CSS-PROJECT-genspark_ai_developer
   start http://localhost:3000
   npm run dev
   pause
   ```
3. 다른 이름으로 저장: `해모아시공관리.bat`
4. 파일 형식: **모든 파일**
5. 저장 위치: 바탕화면
6. 다음부터 이 파일을 더블클릭하면 자동 실행!

---

## 💾 데이터 백업/복구

### 📦 백업 방법:
1. 로그인
2. **대시보드** 페이지 이동
3. 우측 상단 **📦 백업** 버튼 클릭
4. `.db` 파일 다운로드됨
5. USB나 클라우드(Google Drive, OneDrive 등)에 저장

### 🔄 복구 방법:
1. 다른 PC에서 프로젝트 실행
2. 로그인
3. **대시보드** 페이지 이동
4. 우측 상단 **🔄 복구** 버튼 클릭
5. 백업한 `.db` 파일 선택
6. 복구 완료! (자동 새로고침)

---

## ❓ 자주 묻는 질문 (FAQ)

### Q1: "npm을 찾을 수 없습니다" 에러가 나요!
**A**: Node.js가 설치되지 않았거나 설치 후 재시작이 필요합니다.
1. Node.js 다시 설치: https://nodejs.org/ko
2. 컴퓨터 재시작
3. 다시 시도

### Q2: "포트 3000이 이미 사용 중입니다" 에러가 나요!
**A**: 이미 프로그램이 실행 중입니다.
1. 명령 프롬프트 창 모두 닫기
2. 작업 관리자(Ctrl+Shift+Esc) 열기
3. "node.exe" 프로세스 찾아서 종료
4. 다시 `npm run dev` 실행

### Q3: 설치가 너무 오래 걸려요!
**A**: 인터넷 속도에 따라 5~10분 소요됩니다. 기다려주세요!
- 완료되면 자동으로 다음 단계로 진행됩니다

### Q4: 브라우저에서 "페이지를 찾을 수 없습니다" 나와요!
**A**: 
1. 명령 프롬프트에서 서버가 실행 중인지 확인
2. `http://localhost:3000` 정확히 입력했는지 확인
3. 서버 시작 메시지가 나타날 때까지 대기

### Q5: 회사 PC와 집 PC에서 같은 데이터를 쓰려면?
**A**: 
1. 집 PC: 대시보드 → **📦 백업** → USB 저장
2. 회사 PC: 대시보드 → **🔄 복구** → USB 파일 선택

---

## 📞 도움이 필요하면?

1. **에러 메시지 캡처** (스크린샷)
2. **브라우저 콘솔 확인** (F12 키)
3. **GitHub Issues** 등록

---

## 🎉 성공하셨나요?

축하합니다! 이제 해모아 시공관리 시스템을 사용할 수 있습니다! 🚀

---

**작성일**: 2026-01-02  
**버전**: v3.0.0  
**문의**: GitHub Issues
